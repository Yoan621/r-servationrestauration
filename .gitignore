# Thème payant plus utilisé (remplacé par mockup/assets/css/sidebar.css, CSS maison)
mockup/assets/css/app.css

.DS_Store

# Secrets serveur (clé Brevo etc.) — jamais commité
server/.env
node_modules/
